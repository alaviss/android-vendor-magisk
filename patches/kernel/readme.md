# Supporting kernel patches for Magisk integration

This directory contains kernel patches to integrate Magisk modifications into
the kernel.

Apply the patches in the directory correspond to your kernel version.
